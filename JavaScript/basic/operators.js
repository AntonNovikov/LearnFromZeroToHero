// Присвоение
let firstNumber = 10;
let secondNumber = 5;
// Сложение (для строк конкатенация)
let result = firstNumber + secondNumber;

let a = 5;
let b = 10;
let c = 0;

console.log(`a: ${a}, b: ${b}, c: ${c}`);

// c = c + a;
// c = c - a;
// c = c * a;
// c = c / a;
// Сокращенная форма
// c += a;
// c -= a;
// c *= a;
// c /= a;
console.log(`a: ${a}, b: ${b}, c: ${c}`) +
  // typeof  operator, not a function, thus no brackets needed
  // %, ,
  
  // c = 4 ** 2
  
  //  унарные -, +

  "2" +
  "2 sdf";

//
"2" + "work";

// -- ++

// +=, -=, *=
